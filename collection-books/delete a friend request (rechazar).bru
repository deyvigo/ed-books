meta {
  name: delete a friend request (rechazar)
  type: http
  seq: 18
}

delete {
  url: {{BACKEND_URL}}/update/request/friends
  body: json
  auth: none
}

body:json {
  {
    "id_friend": 8
  }
}
